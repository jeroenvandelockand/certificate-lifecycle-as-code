apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: demo-app
spec:
  selector:
    app.kubernetes.io/name: demo-app
  ports:
    - name: https
      port: 443
      targetPort: https

